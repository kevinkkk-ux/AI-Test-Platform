## Ch08 AI协同测试用例生成模块的设计与开发

### 一、本章概述

#### 1.1 学习目标

- 理解AI协同测试用例生成的两种模式：LLM直连模式与RAG增强模式

- 掌握利用大语言模型生成高质量测试用例的工程方法

- 掌握RAG（检索增强生成）技术在测试用例生成中的应用

- 能够独立设计与开发AI协同测试用例生成模块

#### 1.2 相关源码文件

本章涉及的核心源码文件：

- **data_generator.py：**LLM直接生成测试用例
  
- **rag_generator.py：**RAG增强生成测试用例
  
- **rag_store.py：**测试用例向量数据库操作
  
- **database.py：**数据库操作（用例存储）
  
- **main.py：**Gradio界面交互（测试用例生成Tab）

### 二、LLM直接生成测试用例模块

#### 2.1 模块概述

`data_generator.py` 实现了基于大语言模型直接生成测试用例的核心功能，支持登录和注册两种场景的测试用例生成。

#### 2.2 核心依赖与初始化

```python
# data_generator.py
from langchain_core.output_parsers import StrOutputParser
from langchain_deepseek import ChatDeepSeek
from langchain_core.prompts import ChatPromptTemplate
import re
import csv
import io
import pandas as pd
from database import get_db_manager
from config import CASE_GENERATE_TYPE

llm = ChatDeepSeek(model="deepseek-chat", temperature=0.7, streaming=True)
CASE_PATH = "files/testcases.xlsx"
```

**代码解析：**

- **ChatDeepSeek：**LangChain框架中针对DeepSeek模型的封装，支持流式输出

- **temperature=0.7**：设定为0.7，在确定性和创造性之间取得平衡，保证测试用例既规范又全面

- **streaming=True**：开启流式输出，支持实时展示生成过程

- **CASE_PATH**：测试用例Excel文件的保存路径

#### 2.3 Markdown表格解析器

```python
# data_generator.py
def _parse_md_table(md_text: str) -> str:
    """
    解析大模型返回的Markdown格式表格，转换为CSV格式字符串
    """
    if not md_text:
        return ""
    lines = md_text.strip().splitlines()
    data = []
    for line in lines:
        line = line.strip()
        # 跳过空行和分隔行（如 |---|---|）
        if not line.startswith("|") or re.match(r"^\|?\s*-+\s*(\|\s*-+\s*)*\|?$", line):
            continue
        # 跳过表头行（如 | 用例ID | 场景描述 |）
        cells = [c.strip() for c in line.strip("|").split("|")]
        if cells and "用例ID" in cells[0]:
            continue
        data.append(cells)
    if not data:
        return ""
    # 转换为CSV格式
    output = io.StringIO()
    csv.writer(output, lineterminator="\n").writerows(data)
    return output.getvalue()
```

**设计要点：**

- **正则表达式过滤**：`re.match(r"^\|?\s*-+\s*(\|\s*-+\s*)*\|?$", line)` 用于识别并过滤Markdown表格中的分隔行
- **表头识别**：通过判断第一列是否包含"用例ID"来跳过表头
- **CSV格式转换**：使用 `csv.writer` 将解析后的数据转换为标准CSV格式，便于后续处理

#### 2.4 统一用例生成函数

```python
# data_generator.py
def generate_cases(case_type: str, fields: dict, group_num: int = 20, user_id: int = None):
    """
    统一用例生成函数
    :param case_type: 用例类型，如"登录"、"注册"
    :param fields: 字段规则字典，如 {"账号": "大小写字母数字1-20位", "密码": "6-16位字母数字"}
    :param group_num: 生成用例数量
    :param user_id: 用户ID，用于记录操作日志
    :return: (DataFrame, main_id) 生成的用例表格和主表ID
    """
    field_rules = "\n".join([f"{k}：{v}" for k, v in fields.items()])
    table_headers = "用例ID," + ",".join(fields.keys()) + ",场景描述,用例类型,预期结果"
    
    # 构建提示词
    prompt = ChatPromptTemplate.from_messages([
        ("system", f"""你是专业软件测试工程师，使用等价类划分法+健壮边界值法生成{case_type}测试用例。
字段规则：
{field_rules}
硬性要求：
1. 生成{group_num}组用例，所有输入字段互不重复；
2. 包含有效等价类、无效等价类、健壮边界值(合法边界)、健壮边界值(非法边界)四种类型；
3. 输出markdown表格，表头：{table_headers}；
4. 只输出表格，不要任何多余文字。"""),
        ("human", "生成测试用例")
    ])
    
    # 调用大模型
    chain = prompt | llm | StrOutputParser()
    res = chain.invoke({"group_num": group_num})
    csv_text = _parse_md_table(res)
    
    # 解析并验证结果
    cols = ["用例ID"] + list(fields.keys()) + ["场景描述", "用例类型", "预期结果"]
    rows = [r.split(",") for r in csv_text.strip().splitlines() if r.strip()]
    valid_rows = [r for r in rows if len(r) == len(cols)]
    if not valid_rows:
        raise ValueError("LLM返回格式异常")
    
    # 保存到Excel
    df = pd.DataFrame(valid_rows, columns=cols)
    df.to_excel(CASE_PATH, index=False)
    
    # 保存到数据库
    db = get_db_manager()
    main_id = db.insert_test_case_main(
        case_type=case_type, group_num=group_num, 
        case_title=f"{case_type}用例组_{group_num}条",
        case_content=str(fields), generate_type=CASE_GENERATE_TYPE["AI"]
    )
    db.batch_insert_case_detail(main_id, df.to_dict("records"))
    if user_id:
        db.add_operation_log(user_id, "AI用例生成", f"生成{case_type}用例{len(df)}条", main_id)
    
    return df, main_id
```

**核心设计模式——LangChain Chain：**

```
prompt_template → llm_model → output_parser → result
```

- **ChatPromptTemplate**：定义系统提示和用户提示
- **ChatDeepSeek**：大语言模型，执行生成任务
- **StrOutputParser**：解析模型输出为字符串
- **管道操作符 |**：将多个组件串联成处理链

#### 2.5 登录与注册用例生成函数

```python
# data_generator.py
def generate_login_case(account_rule, pwd_rule, group_num=20, user_id=None):
    """生成登录测试用例"""
    return generate_cases("登录", {"账号": account_rule, "密码": pwd_rule}, group_num, user_id)

def generate_register_case(account_rule, pwd_rule, email_rule, phone_rule, group_num=20, user_id=None):
    """生成注册测试用例"""
    return generate_cases("注册", {
        "账号": account_rule, 
        "密码": pwd_rule, 
        "确认密码": "与密码一致", 
        "邮箱": email_rule, 
        "手机号": phone_rule
    }, group_num, user_id)
```

**设计模式——工厂方法模式：**

- `generate_cases` 是核心工厂方法

- generate_login_case 和 generate_register_case是具体工厂函数
  
- 这种设计提高了代码复用性，便于扩展新的用例类型

### 三、RAG增强生成测试用例模块（扩展功能）

#### 3.1 模块概述

`rag_generator.py` 实现了基于RAG（检索增强生成）技术的测试用例生成，相比LLM直接生成，其优势在于：

- 利用历史测试用例库作为参考，提高生成质量
- 支持上传需求文档，使生成结果更贴合业务场景
- 实现"检索+生成"的双重保障

#### 3.2 文档加载器

```python
# rag_generator.py
import os
import pandas as pd
from typing import List, Tuple
from langchain_core.output_parsers import StrOutputParser
from langchain_deepseek import ChatDeepSeek
from langchain_core.prompts import ChatPromptTemplate
from langchain_community.document_loaders import TextLoader, PyPDFLoader
from langchain_community.vectorstores import FAISS
from langchain_huggingface import HuggingFaceEmbeddings
from langchain_text_splitters import RecursiveCharacterTextSplitter
from data_generator import _parse_md_table
from database import get_db_manager
from config import CASE_GENERATE_TYPE, PATH_CONFIG

llm = ChatDeepSeek(model="deepseek-chat", temperature=0.3)

def load_documents(file_paths: List[str]) -> List[dict]:
    """
    加载多种格式的文档文件
    :param file_paths: 文件路径列表
    :return: 文档内容列表，每项包含content和source
    """
    docs = []
    for path in file_paths:
        if not os.path.exists(path):
            continue
        ext = os.path.splitext(path)[1].lower()
        try:
            if ext == '.txt':
                loaded = TextLoader(path, encoding='utf-8').load()
            elif ext == '.pdf':
                loaded = PyPDFLoader(path).load()
            elif ext == '.docx':
                from langchain_community.document_loaders import Docx2txtLoader
                loaded = Docx2txtLoader(path).load()
            else:
                continue
            for doc in loaded:
                docs.append({"content": doc.page_content, "source": os.path.basename(path)})
        except Exception as e:
            print(f"加载失败 {path}: {e}")
    return docs
```

**文档加载器架构：**

```
文件路径 → 文件类型判断 → 对应加载器 → 文档对象列表
```

- **TXT文件：**使用 TextLoader，纯文本加载

- **PDF文件**：使用 `PyPDFLoader`，处理PDF格式

- **DOCX文件：**使用 Docx2txtLoader，处理Word文档

#### 3.3 向量存储构建

```python
# rag_generator.py
def build_vector_store(chunks: List[dict], store_path: str = None, user_id: int = None):
    """
    构建FAISS向量存储
    :param chunks: 文档切片列表
    :param store_path: 存储路径
    :param user_id: 用户ID
    """
    store_path = store_path or PATH_CONFIG["faiss_vector_store"]
    os.makedirs(store_path, exist_ok=True)
    embeddings = HuggingFaceEmbeddings(
        model_name=PATH_CONFIG["embed_model_path"], 
        model_kwargs={'device': 'cpu'}
    )
    texts = [c["content"] for c in chunks]
    metadatas = [{"source": c["source"]} for c in chunks]
    FAISS.from_texts(texts=texts, embedding=embeddings, metadatas=metadatas).save_local(store_path)

def load_vector_store(store_path: str = None) -> FAISS:
    """
    加载本地FAISS向量存储
    """
    store_path = store_path or PATH_CONFIG["faiss_vector_store"]
    embeddings = HuggingFaceEmbeddings(
        model_name=PATH_CONFIG["embed_model_path"], 
        model_kwargs={'device': 'cpu'}
    )
    return FAISS.load_local(store_path, embeddings, allow_dangerous_deserialization=True)
```

**向量化流程：**

```
文档切片 → HuggingFaceEmbeddings(嵌入模型) → 文本向量化 → FAISS存储
```

#### 3.4 检索上下文函数

```python
# rag_generator.py
def retrieve_context(query: str, top_k: int = 5) -> str:
    """
    从向量库检索相关文档
    :param query: 查询文本
    :param top_k: 返回最相似的文档数量
    :return: 检索到的文档文本
    """
    try:
        vs = load_vector_store()
        docs = vs.similarity_search(query, k=top_k)
        return "\n\n".join([f"[{i+1}] {d.page_content}" for i, d in enumerate(docs)])
    except:
        return ""
```

**RAG检索流程：**

```
用户查询 → 向量化 → FAISS相似度搜索 → 返回Top-K文档
```

#### 3.5 RAG增强生成核心函数

```python
# rag_generator.py
def _rag_generate(case_type: str, fields: dict, group_num: int, query: str, user_id: int = None) -> Tuple[pd.DataFrame, int]:
    """
    RAG增强生成测试用例核心函数
    :param case_type: 用例类型
    :param fields: 字段规则
    :param group_num: 生成数量
    :param query: 检索查询
    :param user_id: 用户ID
    """
    # 第一步：从向量库检索相关文档
    context = retrieve_context(query)
    
    field_rules = "\n".join([f"{k}：{v}" for k, v in fields.items()])
    table_headers = "用例ID," + ",".join(fields.keys()) + ",场景描述,用例类型,预期结果"
    
    # 第二步：构建增强提示词（将检索结果作为上下文注入）
    prompt = ChatPromptTemplate.from_messages([
        ("system", f"""你是专业测试工程师，参考需求文档生成{case_type}测试用例。
需求文档参考：
{context}  # ← 注入检索到的相关文档
字段规则：
{field_rules}
硬性要求：
1. 生成{group_num}组用例，所有输入字段互不重复；
2. 包含有效等价类、无效等价类、健壮边界值(合法边界)、健壮边界值(非法边界)四种类型；
3. 输出markdown表格，表头：{table_headers}；
4. 只输出表格，不要任何多余文字。"""),
        ("human", "生成测试用例")
    ])
    
    # 第三步：LLM生成
    res = (prompt | llm | StrOutputParser()).invoke({})
    csv_text = _parse_md_table(res)
    
    # 第四步：解析并保存结果
    cols = ["用例ID"] + list(fields.keys()) + ["场景描述", "用例类型", "预期结果"]
    rows = [r.split(",") for r in csv_text.strip().splitlines() if r.strip()]
    valid_rows = [r for r in rows if len(r) == len(cols)]
    if not valid_rows:
        raise ValueError("格式异常")
    
    df = pd.DataFrame(valid_rows, columns=cols)
    db = get_db_manager()
    main_id = db.insert_test_case_main(
        case_type=case_type, group_num=group_num,
        case_title=f"RAG{case_type}用例组{group_num}条",
        case_content=str(fields), generate_type=CASE_GENERATE_TYPE["RAG"]
    )
    db.batch_insert_case_detail(main_id, df.to_dict("records"))
    if user_id:
        db.add_operation_log(user_id, "RAG用例生成", f"生成{case_type}用例{len(df)}条", main_id)
    return df, main_id
```

**RAG生成流程对比：**

| 步骤 | LLM直接生成    | RAG增强生成                  |
| ---- | -------------- | ---------------------------- |
| 1    | 构建基础提示词 | **检索相关文档**             |
| 2    | 调用LLM生成    | 构建增强提示词（含检索结果） |
| 3    | 解析结果       | 调用LLM生成                  |
| 4    | 保存结果       | 解析结果                     |
| 5    | -              | 保存结果                     |

#### 3.6 登录与注册RAG生成函数

```python
# rag_generator.py
def rag_generate_login_case(account_rule, pwd_rule, group_num=20, doc_paths=None, query="登录功能", user_id=None):
    """
    RAG增强生成登录测试用例
    :param doc_paths: 上传的需求文档路径列表
    """
    if doc_paths:
        # 如果有上传文档，先加载并构建向量库
        docs = load_documents(doc_paths)
        chunks = [{"content": d["content"], "source": d["source"]} for d in docs]
        build_vector_store(chunks, user_id=user_id)
    return _rag_generate("登录", {"账号": account_rule, "密码": pwd_rule}, group_num, query, user_id)

def rag_generate_register_case(account_rule, pwd_rule, email_rule, phone_rule, group_num=20, doc_paths=None, query="注册功能", user_id=None):
    """
    RAG增强生成注册测试用例
    """
    if doc_paths:
        docs = load_documents(doc_paths)
        chunks = [{"content": d["content"], "source": d["source"]} for d in docs]
        build_vector_store(chunks, user_id=user_id)
    return _rag_generate("注册", {
        "账号": account_rule, 
        "密码": pwd_rule, 
        "确认密码": "与密码一致", 
        "邮箱": email_rule, 
        "手机号": phone_rule
    }, group_num, query, user_id)
```

### 四、测试用例向量存储模块

#### 4.1 模块概述

`rag_store.py` 实现了基于ChromaDB的测试用例向量存储，用于：

- 存储历史测试用例的向量表示

- 支持基于语义相似度的检索

- 为RAG生成提供知识库支持

#### 4.2 核心类实现

```python
# rag_store.py
from typing import List, Dict, Any
import chromadb
from chromadb.utils import embedding_functions
import logging
import uuid
from config import PATH_CONFIG

logger = logging.getLogger(__name__)

class TestCaseRAG:
    def __init__(self):
        # 初始化嵌入函数（将文本转为向量）
        self.embed_func = embedding_functions.SentenceTransformerEmbeddingFunction(
            model_name=PATH_CONFIG["embed_model_path"]
        )
        # 初始化ChromaDB客户端（持久化存储）
        self.client = chromadb.PersistentClient(path=PATH_CONFIG["chroma_db_root"])
        # 获取或创建集合
        self.collection = self.client.get_or_create_collection(
            name="test_case_knowledge", 
            embedding_function=self.embed_func
        )
    
    def add_test_case_batch(self, case_list: List[Dict]) -> int:
        """
        批量添加测试用例到向量库
        :param case_list: 测试用例字典列表
        :return: 添加的用例数量
        """
        docs, metas, ids = [], [], []
        gen_type_map = {'1': 'AI生成', '2': 'RAG生成', '3': '手动录入', '4': '接口导入'}
        for case in case_list:
            cid = case.get("case_id") or f"null_{uuid.uuid4().hex[:8]}"
            # 构建文档文本
            text = f"用例ID: {case.get('case_id', '无')}\n" \
                   f"标题: {case.get('case_title', '无')}\n" \
                   f"描述: {case.get('case_content', '无')}\n" \
                   f"生成类型: {gen_type_map.get(str(case.get('case_generate_type', '')), '未知')}\n" \
                   f"用例类型: {case.get('case_type', '未分类')}"
            docs.append(text)
            metas.append({
                "case_id": str(case.get("case_id")),
                "title": case.get("case_title"),
                "case_type": case.get("case_type")
            })
            ids.append(f"case_{cid}")
        if docs:
            self.collection.add(documents=docs, metadatas=metas, ids=ids)
        return len(docs)
    
    def retrieve(self, query: str, top_k: int = 3) -> str:
        """
        根据查询文本检索最相似的测试用例
        :param query: 查询文本
        :param top_k: 返回结果数量
        :return: 检索到的文档文本
        """
        res = self.collection.query(query_texts=[query], n_results=top_k)
        docs = res.get("documents", [[]])
        if not docs:
            return "未找到相关历史用例"
        return "\n".join([f"【参考用例 {i+1}】\n{d}" for i, d in enumerate(docs)])
    
    def count(self) -> int:
        """返回向量库中的用例总数"""
        return self.collection.count()

# 全局单例
rag_case_store = TestCaseRAG()
```

**ChromaDB数据结构：**

```
Collection: test_case_knowledge
├── documents: ["用例文本1", "用例文本2", ...]
├── metadatas: [{"case_id": "1", "title": "..."}, ...]
└── ids: ["case_1", "case_2", ...]
```

#### 4.3 系统初始化时同步历史用例

```python
# main.py 第54-60行（init_system函数）
def init_system():
    global analysis_agent
    db = get_db_manager()
    if not db.connect():
        raise Exception("MySQL连接失败")
    sql_gen = SQLGenerator()
    analyzer = DataAnalyzer()
    analysis_agent = DataAnalysisAgent(sql_gen, db, analyzer, visualizer, report_generator)
    # 同步历史用例到向量库
    ok, cases = db.execute_query(
        "SELECT t.*, COALESCE(u.nickname, u.username) as user_name FROM test_case t "
        "LEFT JOIN sys_user u ON t.user_id = u.user_id WHERE t.case_is_deleted=0"
    )
    if ok and cases:
        cnt = rag_case_store.add_test_case_batch(cases)
        logger.info(f"同步{cnt}条历史用例至向量库")
```

### 五、数据库操作模块（用例存储相关）

#### 5.1 测试用例主表插入

```python
# database.py
def insert_test_case_main(self, **kwargs) -> int:
    """
    插入测试用例主表记录
    :return: 主表ID
    """
    sql = """INSERT INTO test_case(case_title, case_content, case_type, case_generate_type, 
             case_create_time, case_is_deleted) 
             VALUES (%s, %s, %s, %s, NOW(), 0)"""
    try:
        with self.get_cursor(dictionary=False) as cursor:
            cursor.execute(sql, (
                kwargs.get('case_title', ''),
                kwargs.get('case_content', ''),
                kwargs.get('case_type', ''),
                kwargs.get('generate_type', 1)
            ))
            return cursor.lastrowid
    except Exception as e:
        logger.error(f"插入用例主表失败:{e}")
        return 0
```

#### 5.2 测试用例详情批量插入

```python
# database.py
def batch_insert_case_detail(self, main_id: int, detail_list: List[Dict]) -> bool:
    """
    批量插入测试用例详情
    :param main_id: 主表ID
    :param detail_list: 用例详情列表
    """
    sql = """INSERT INTO test_case(case_title, case_content, case_type, case_generate_type, 
             case_create_time, case_is_deleted) 
             VALUES (%s, %s, %s, %s, NOW(), 0)"""
    try:
        with self.get_cursor(dictionary=False) as cursor:
            for item in detail_list:
                cursor.execute(sql, (
                    item.get("场景描述") or item.get("scene_desc", ""),
                    f"测试步骤: {item.get('测试步骤') or item.get('test_step', '')}\n"
                    f"预期结果: {item.get('预期结果') or item.get('expect_result', '')}",
                    item.get("用例类型") or item.get("case_category", ""),
                    1
                ))
            return True
    except Exception as e:
        logger.error(f"批量插入用例明细失败:{e}")
        return False
```

### 六、Gradio界面交互（测试用例生成Tab）

#### 6.1 界面布局

```python
# main.py
with gr.TabItem("测试用例生成"):
    # 文档上传与向量库构建
    with gr.Row():
        doc_upload = gr.File(
            label="上传需求文档", 
            file_count="multiple", 
            file_types=[".pdf", ".txt", ".docx"]
        )
        build_btn = gr.Button("构建FAISS向量库", variant="primary")
    vec_msg = gr.Textbox(label="构建日志", lines=2)
    
    # 构建向量库事件
    build_btn.click(build_faiss, [doc_upload], [vec_msg])
    
    # LLM生成与RAG生成子Tab
    with gr.Tabs():
        # LLM生成子Tab
        with gr.TabItem("LLM生成"):
            # 登录用例生成
            with gr.TabItem("登录用例"):
                l_acc = gr.Textbox(label="账号规则", value="大小写字母数字1-20位")
                l_pwd = gr.Textbox(label="密码规则", value="6-16位字母数字")
                l_num = gr.Slider(10, 50, 20, label="生成条数")
                gen_l = gr.Button("生成", variant="primary")
                l_table = gr.Dataframe(label="登录用例", interactive=True)
                gen_l.click(
                    lambda a, p, n, r=None: generate_login_case(a, p, n)[0], 
                    [l_acc, l_pwd, l_num], [l_table]
                )
            
            # 注册用例生成
            with gr.TabItem("注册用例"):
                r_acc = gr.Textbox(label="账号规则", value="大小写字母数字1-20位")
                r_pwd = gr.Textbox(label="密码规则", value="6-16位字母数字")
                r_email = gr.Textbox(label="邮箱规则", value="标准邮箱格式")
                r_phone = gr.Textbox(label="手机号规则", value="11位手机号")
                r_num = gr.Slider(10, 50, 20, label="生成条数")
                gen_r = gr.Button("生成", variant="primary")
                r_table = gr.Dataframe(label="注册用例", interactive=True)
                gen_r.click(
                    lambda a, p, e, ph, n, r=None: generate_register_case(a, p, e, ph, n)[0],
                    [r_acc, r_pwd, r_email, r_phone, r_num], [r_table]
                )
```

#### 6.2 RAG生成子Tab

```python
# main.py
with gr.TabItem("RAG生成"):
    with gr.TabItem("RAG登录"):
        rl_acc = gr.Textbox(label="账号规则", value="大小写字母数字1-20位")
        rl_pwd = gr.Textbox(label="密码规则", value="6-16位字母数字")
        rl_query = gr.Textbox(label="检索关键词", value="登录功能校验规则")
        rl_num = gr.Slider(10, 50, 20, label="生成条数")
        gen_rl = gr.Button("生成", variant="primary")
        rl_table = gr.Dataframe(label="登录用例")
        rl_msg = gr.Textbox(label="提示")
        gen_rl.click(
            lambda a, p, n, q, d, r=None: (
                rag_generate_login_case(a, p, n, [f.name for f in d] if d else None, q)[0], 
                "生成成功"
            ),
            [rl_acc, rl_pwd, rl_num, doc_upload, rl_query], 
            [rl_table, rl_msg]
        )
    
    with gr.TabItem("RAG注册"):
        # 类似RAG登录的配置
        rr_acc = gr.Textbox(label="账号规则", value="大小写字母数字1-20位")
        rr_pwd = gr.Textbox(label="密码规则", value="6-16位字母数字")
        rr_email = gr.Textbox(label="邮箱规则", value="标准邮箱格式")
        rr_phone = gr.Textbox(label="手机号规则", value="11位手机号")
        rr_query = gr.Textbox(label="检索关键词", value="注册功能校验规则")
        rr_num = gr.Slider(10, 50, 20, label="生成条数")
        gen_rr = gr.Button("生成", variant="primary")
        rr_table = gr.Dataframe(label="注册用例")
        rr_msg = gr.Textbox(label="提示")
        gen_rr.click(
            lambda a, p, e, ph, n, q, d, r=None: (
                rag_generate_register_case(a, p, e, ph, n, [f.name for f in d] if d else None, q)[0],
                "生成成功"
            ),
            [rr_acc, rr_pwd, rr_email, rr_phone, rr_num, doc_upload, rr_query],
            [rr_table, rr_msg]
        )
```

### 七、完整数据流分析

#### 7.1 LLM直接生成流程图

```
用户输入规则
    ↓
generate_cases() 构建提示词
    ↓
Chain: prompt_template → llm → output_parser
    ↓
生成Markdown表格
    ↓
_parse_md_table() 解析为CSV
    ↓
pandas.DataFrame 转为结构化数据
    ↓
db.insert_test_case_main() 保存主表
    ↓
db.batch_insert_case_detail() 保存详情
    ↓
返回DataFrame和主表ID
```

#### 7.2 RAG增强生成流程图

```
用户上传文档 → load_documents() → 文本切片
    ↓
build_vector_store() → FAISS向量库
    ↓
用户输入规则 + 检索关键词
    ↓
retrieve_context() → 检索相关文档
    ↓
_rag_generate() 构建增强提示词
    ↓
Chain: prompt_template(含检索结果) → llm → output_parser
    ↓
... 后续与LLM生成相同 ...
```

### 八、本章小结

通过本章学习，我们掌握了：

1. **LLM直接生成**：利用LangChain框架构建提示词链，调用大模型生成测试用例

2. **RAG增强生成：**结合向量检索和LLM生成，提高用例的准确性和业务相关性

3. **向量存储**：使用ChromaDB存储和检索测试用例向量

4. **数据库操作**：将生成的用例持久化到MySQL数据库

5. **界面交互**：通过Gradio界面实现用户友好的用例生成操作

**关键架构设计模式：**

- **工厂模式**：`generate_cases` 作为统一工厂函数

- **链式调用**：LangChain的管道操作符 `|` 实现组件串联

- **RAG模式：**检索增强生成，提升生成质量

- **持久化存储**：同时保存到Excel和数据库，保证数据安全